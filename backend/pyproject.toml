[project]
name = "users-insights-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "httpx",
    "python-dotenv",
]

[dependency-groups]
dev = [
    "pytest",
    "requests-mock",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
